{
  "name": "server",
  "version": "0.0.1",
  "description": "raidmanager server",
  "main": "server.js",
  "scripts": {
    "sql-apply": "psql postgres < ./sql/schema.sql",
    "sql-dump": "pg_dump --schema-only postgres > ./sql/schema.sql",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "db:import": "bash scripts/db-import.sh"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.11.0"
  }
}
